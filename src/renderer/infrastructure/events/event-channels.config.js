/**
 * Compatibility re-export for event channels.
 *
 * Canonical source now lives in renderer/common/config/event-channels.ts.
 */
export { EventChannels } from '@renderer/common/config/event-channels';
