# agy-phase config — autonomous execution of the committed PLAN-01..04 (repo root).
# Each phase branches off the accumulating base; the orchestrator (Claude) verifies
# against git + re-runs the plan's gates, then squash-merges to the base before chaining.
AGY_BASE='refactor/codebase_reduction'
AGY_BRANCH_TMPL='refactor/plan-{P}'
AGY_ORCH=''
AGY_TIMEOUT='180m'
