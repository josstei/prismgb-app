/**
 * Shared Vitest setup entrypoint.
 *
 * Browser API mocks are now provided by project-specific setup modules under
 * tests/support/mocks, so this file only preserves a minimal shared hook.
 */

export {};
